{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "lib": ["ESNext", "DOM"],
        "types": ["bun"]
    },
    "include": ["convex", "scripts"],
    "exclude": ["node_modules"]
}
